import { Button, FormControl, Paper, Stack, Typography } from "@mui/material";

export default function LoginForm() {
  return (
    <Stack
      sx={{
        background: "red",
      }}
    >
      <Stack
        sx={{
          background: "red",
        }}
      >
        
        <Button variant="contained"> AAA </Button>
      </Stack>
    </Stack>
  );
}
